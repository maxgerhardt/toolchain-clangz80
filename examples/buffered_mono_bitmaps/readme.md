### Example of Buffered Monochrome Bitmaps

Loads a large monochrome bitmap's data from memory into a buffer. File is raw binary data.

Makes dupliacte of buffer and creates coloured bitmap.

Displays tinted bitmap on screen.

Press C to change colour.
Press ESC to exit the program.
